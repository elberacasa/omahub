#!/bin/bash

# omahub:title=Automatically hide
# omahub:summary=The dock slides away when a window reaches it, and its edge of the screen brings it back
# omahub:section=dock
# omahub:order=40
# omahub:kind=toggle
# omahub:icon=󰁅
# omahub:keywords=dock hide autohide automatically edge reveal slide away

set -euo pipefail
source "${OMAHUB_PATH:?run this through omahub}/lib/settings.sh"
source "$OMAHUB_PATH/lib/dock.sh"

omahub_dock_toggle_setting autohide true dock/autohide "$@"
