#!/bin/bash

# omahub:title=GPT's pet
# omahub:summary=The pet every GPT model gets on its agent tile, in Codex and anywhere else
# omahub:section=dock
# omahub:order=69
# omahub:kind=choice
# omahub:preview=pet
# omahub:icon=󰏩
# omahub:keywords=dock agents pets pet gallery gpt openai codex blob cat gem bunny fox owl robot
# omahub:requires=codex

set -euo pipefail
source "${OMAHUB_PATH:?run this through omahub}/lib/settings.sh"
source "$OMAHUB_PATH/lib/dock.sh"

omahub_dock_choice_setting pet-openai cat "blob=Blob cat=Cat gem=Gem bunny=Bunny fox=Fox owl=Owl robot=Robot" dock/pet-gpt "$@"
