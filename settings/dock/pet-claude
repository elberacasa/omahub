#!/bin/bash

# omahub:title=Claude's pet
# omahub:summary=The pet every Claude model gets on its agent tile
# omahub:section=dock
# omahub:order=68
# omahub:kind=choice
# omahub:preview=pet
# omahub:icon=󰏩
# omahub:keywords=dock agents pets pet gallery claude anthropic blob cat gem bunny fox owl robot
# omahub:requires=claude

set -euo pipefail
source "${OMAHUB_PATH:?run this through omahub}/lib/settings.sh"
source "$OMAHUB_PATH/lib/dock.sh"

omahub_dock_choice_setting pet-anthropic blob "blob=Blob cat=Cat gem=Gem bunny=Bunny fox=Fox owl=Owl robot=Robot" dock/pet-claude "$@"
