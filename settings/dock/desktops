#!/bin/bash

# omahub:title=Desktops in the dock
# omahub:summary=Your desktops sit at the end of the dock. Click one to go there, or drop an app on one to open it there
# omahub:section=dock
# omahub:order=65
# omahub:kind=toggle
# omahub:icon=󰀻
# omahub:keywords=dock desktops workspaces spaces switch open app on desktop

set -euo pipefail
source "${OMAHUB_PATH:?run this through omahub}/lib/settings.sh"
source "$OMAHUB_PATH/lib/dock.sh"

omahub_dock_toggle_setting desktops false dock/desktops "$@"
