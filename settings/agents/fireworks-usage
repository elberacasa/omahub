#!/bin/bash

# omahub:title=Fireworks usage
# omahub:summary=Shows your Fireworks balance and daily tokens in the bar's Agents panel
# omahub:section=agents
# omahub:kind=toggle
# omahub:icon=󰊚
# omahub:keywords=fireworks subscription balance credits usage tokens bar panel meter
# omahub:requires=omarchy-agent-usage-fireworks

set -euo pipefail
source "${OMAHUB_PATH:?run this through omahub}/lib/settings.sh"
source "$OMAHUB_PATH/lib/agents.sh"

omahub_agents_toggle_setting fireworks agents/fireworks-usage "$@"
