#!/bin/bash

# omahub:title=Claude on the bar
# omahub:summary=Which Claude limit the bar shows, with Auto for the weekly limit
# omahub:section=agents
# omahub:kind=choice
# omahub:icon=󰄨
# omahub:keywords=claude anthropic session weekly fable opus model bar limit fullest percent subscription
# omahub:requires=omarchy-agent-usage-claude

set -euo pipefail
source "${OMAHUB_PATH:?run this through omahub}/lib/settings.sh"
source "$OMAHUB_PATH/lib/agents.sh"

omahub_agents_bar_limit_setting claude agents/claude-bar-limit "$@"
