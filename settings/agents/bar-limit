#!/bin/bash

# omahub:title=Limit on the bar
# omahub:summary=Which limit the bar shows for each subscription. Auto shows the weekly limit unless a shorter one is nearly full
# omahub:section=agents
# omahub:kind=choice
# omahub:icon=󰄨
# omahub:keywords=bar limit weekly session fullest model fable opus percent claude codex subscription
# omahub:requires=omarchy-agent-usage-update

set -euo pipefail
source "${OMAHUB_PATH:?run this through omahub}/lib/settings.sh"
source "$OMAHUB_PATH/lib/agents.sh"

omahub_agents_bar_limit_setting agents/bar-limit "$@"
