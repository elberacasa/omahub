#!/bin/bash

# omahub:title=Codex usage
# omahub:summary=Shows your Codex limits and daily tokens in the bar's Agents panel
# omahub:section=agents
# omahub:kind=toggle
# omahub:icon=󰊚
# omahub:keywords=codex openai chatgpt subscription plan usage limits tokens bar panel meter
# omahub:requires=omarchy-agent-usage-codex

set -euo pipefail
source "${OMAHUB_PATH:?run this through omahub}/lib/settings.sh"
source "$OMAHUB_PATH/lib/agents.sh"

omahub_agents_toggle_setting codex agents/codex-usage "$@"
