#!/bin/bash

# omahub:title=Agent keys
# omahub:summary=Agent on SUPER + SHIFT + A, browser on SUPER + B, and dictation on SUPER + R
# omahub:section=keyboard
# omahub:kind=toggle
# omahub:icon=󰚩
# omahub:keywords=agent ai browser dictation voice voxtype chatgpt shortcut

set -euo pipefail
source "${OMAHUB_PATH:?run this through omahub}/lib/settings.sh"
source "$OMAHUB_PATH/lib/keymap.sh"

omahub_keymap_toggle_setting agents keyboard/agent-keys "$@"
