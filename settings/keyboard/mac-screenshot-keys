#!/bin/bash

# omahub:title=Mac screenshot keys
# omahub:summary=SUPER + SHIFT + 3, 4, and 5 take screenshots, like a Mac
# omahub:section=keyboard
# omahub:kind=toggle
# omahub:icon=󰹑
# omahub:keywords=screenshot capture keyboard mac layout workspace move

set -euo pipefail
source "${OMAHUB_PATH:?run this through omahub}/lib/settings.sh"
source "$OMAHUB_PATH/lib/keymap.sh"

omahub_keymap_toggle_setting mac keyboard/mac-screenshot-keys "$@"
